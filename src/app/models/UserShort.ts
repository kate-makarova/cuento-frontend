export class UserShort {
  id: number;
  userName: string;
  avatar: string;

  constructor(id: number, userName: string, avatar: string) {
    this.id = id;
    this.userName = userName;
    this.avatar = avatar;
  }
}
